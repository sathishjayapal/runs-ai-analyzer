spring:
  application:
    name: runs-ai-analyzer
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

# Note: Main configuration is loaded from Spring Cloud Config Server
# See: jubilant-memory/running/runs-ai-analyzer/runs-ai-analyzer-{profile}.yml
